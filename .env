# OLLAMA runs locally — no API keys needed!
# OLLAMA_HOST defaults to http://localhost:11434 if not set
OLLAMA_HOST=http://localhost:11434

# PostgreSQL + pgvector connection string
# Default works with the Docker command in README
DATABASE_URL=postgresql+psycopg://ai:ai@localhost:5532/ai
